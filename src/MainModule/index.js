import * as createjs from 'createjs-module';
import * as _ from 'lodash';
import stage from './Stage';

const SHARED_DEPENDENCIES = {
  stage,
  createjs,
  _,
};

export default SHARED_DEPENDENCIES;
